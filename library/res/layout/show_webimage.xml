<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="fill_parent"
              android:layout_height="fill_parent"
              android:orientation="vertical" >

    <!-- TODO 默认占位图 -->

    <com.trs.app.ZoomableImageView
            android:id="@+id/show_webimage_imageview"
            android:layout_width="fill_parent"
            android:layout_height="@dimen/size300"
            android:layout_alignParentTop="true"
            android:layout_marginTop="@dimen/size100"
            android:scaleType="matrix"
            android:src="@drawable/icon_b_submit" />
<!--
    <TextView
            android:id="@+id/show_webimage_imagepath_textview"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_below="@id/show_webimage_imageview"
            android:gravity="center"
            android:textColor="#ffff0000" />
-->
    <ImageView android:id="@+id/save_btn"
               android:layout_centerHorizontal="true"
               android:layout_below="@id/show_webimage_imageview"
               android:src="@drawable/icon_b_submit"
               android:layout_width="wrap_content"
               android:layout_height="wrap_content" />

</RelativeLayout>